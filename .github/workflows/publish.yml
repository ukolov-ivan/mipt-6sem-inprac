on:
  push:
    branches:
      - master
      - release/*
  workflow_dispatch:

jobs:
  backend:
    uses: ./.github/workflows/backend-ci.yml@main

  frontend:
    uses: ./.github/workflows/backend-ci.yml@main

  job3:
    runs-on: ubuntu-latest
    needs: [backend, frontend]
    steps:
